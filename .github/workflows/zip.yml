name: Create Archive
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Upload Archive
      uses: actions/upload-artifact@v2
      with:
        name: ResourcePack
        path:  ${{ github.workspace }}
